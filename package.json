{
  "name": "nuvora",
  "version": "1.0.0",
  "description": "NUVORA — Global real-time messaging platform",
  "scripts": {
    "build": "node -c script.js && echo \"Build successful: static site verified\""
  }
}
